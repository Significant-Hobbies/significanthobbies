{
  "name": "significant-hobbies-hub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:worker": "npm --prefix services/hub-backend ci",
    "check": "npm --prefix services/hub-backend run check",
    "test:worker": "npm --prefix services/hub-backend test",
    "test:swift": "swift test"
  },
  "engines": {
    "node": ">=22"
  }
}
